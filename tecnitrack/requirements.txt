# TecniTrack - Dependencias
# Schema-based multi-tenancy con django-tenants

django>=4.2,<5.0
django-tenants>=3.5.0      # Motor de multi-tenancy por schema PostgreSQL
psycopg2-binary>=2.9       # Driver PostgreSQL (OBLIGATORIO con django-tenants)
Pillow>=10.0               # Manejo de imagenes
gunicorn>=21.0             # Servidor WSGI para produccion
whitenoise>=6.6            # Archivos estaticos en produccion
python-dotenv>=1.0         # Variables de entorno desde .env

# NO incluir SQLite drivers — django-tenants requiere PostgreSQL
